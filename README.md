# redis-vector-cache-demo
A high-performance LLM gateway built with **FastAPI**, **Redis Stack**, and **OpenAI** that intercepts prompt requests, matches semantic intent via in-memory vector search, and delivers **5ms response times** while slashing API costs.
